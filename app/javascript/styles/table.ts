import styled from 'styled-components';
import { Table as Tbl, Thead as Thd, Tbody as Tbdy, TableContainer as Container, Tr as TableRow, Td as TableCell, Th as TableHeader } from '@chakra-ui/react'

export const TableContainer = styled(Container)`
  
`;

export const Table = styled(Tbl)`
`;

export const Thead = styled(Thd)`

`;

export const Tbody = styled(Tbdy)`

`;

export const Tr = styled(TableRow)`

`;

export const Th = styled(TableHeader)`

`;

export const Td = styled(TableCell)`

`;